#include <stdio.h>

int main()
{
  int instances = 0;
  scanf("%d", &instances);

  for (int z = 0; z < instances; ++z)
  {
    int vertices = 0;
    int edges = 0;
    scanf("%d %d", &vertices, &edges);

    for (int g = 0; g < edges; ++g)
    {
    }
  }
}